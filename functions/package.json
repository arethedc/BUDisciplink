{
  "name": "apps-functions",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "node ../scripts/sync_firebase_target.js && firebase deploy --only functions",
    "serve": "node ../scripts/sync_firebase_target.js && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@genkit-ai/googleai": "^1.10.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "genkit": "^1.10.0"
  }
}
